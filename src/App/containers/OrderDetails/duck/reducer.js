import { format } from "prettier";
import { createReducer } from "@reduxjs/toolkit";

const initialValue = {
  orderId: 50011854090701,
};

const orderReducer = createReducer(initialValue, {
  //[]: return state;
});
